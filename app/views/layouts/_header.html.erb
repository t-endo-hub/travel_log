<nav class="navbar navbar-expand-md  navbar-light bg-light">
  <a class="navbar-brand" href="/"><img src="images/others/logo.png" alt="ロゴ"></a>
  <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#bs-navi" aria-controls="bs-navi" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <% if user_signed_in? %>
    <p>
      ようこそ
      <%= link_to current_user.name, user_path(current_user) %>
      さん！
    </p>
  <% end %>

  <div class="collapse navbar-collapse" id="bs-navi">
    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <i class="fas fa-list-ul"></i>
          ジャンルから探す
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <% Genre.all.each do |genre| %>
            <%= link_to genre.name, tourist_spot_genre_search_path(genre), class: "dropdown-item" %>
          <% end %>
        </div>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <i class="fas fa-bullseye"></i>
          利用シーンから探す
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <% Scene.all.each do |scene| %>
            <%= link_to scene.name, tourist_spot_scene_search_path(scene), class: "dropdown-item" %>
          <% end %>
        </div>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <i class="fas fa-globe-asia"></i>
          都道府県から探す
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <% JpPrefecture::Prefecture.all.each do |prefecture| %>
            <%= link_to prefecture.name, tourist_spot_prefecture_search_path(prefecture_search: prefecture.code), class: "dropdown-item" %>
          <% end %>
        </div>
      </li>
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            アカウント
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to user_path(current_user) do %>
              <i class="fas fa-user"></i>
              <span>マイページ</span>
            <% end %>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</nav>