<% if user_signed_in? %>
  <% if current_user.following?(user) %>
    <%= link_to 'フォロー解除', unfollow_path(user.id), method: :POST, class: 'btn btn-info' %>
  <% else %>
    <%= link_to 'フォロー', follow_path(user.id), method: :POST, class: 'btn btn-default' %>
  <% end %>
<% end %>